1.Download the BestCarParkingModel2.py in your computer

2.Open VS code and run the ArtificathonWebsite.py code. Do the pip imports: from ultralytics import YOLO  AND PIL import Image need to be downloaded with Pip. The can be seen in the top of the code.

3.run "streamlit run ./ArtificathonWebsite.py" in the Terminal of VS code

4.It should work(The code uses streamlit to run the model)
