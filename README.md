1.Download the BestCarParkingModel2.py
2.Open VS code and run the ArtificathonWebsite.py code
from ultralytics import YOLO
from PIL import Image need to be downloaded with Pip
3. run streamlit run ./ArtificathonWebsite.py in the Terminal
4.It should work(The code uses streamlit to run the model)
